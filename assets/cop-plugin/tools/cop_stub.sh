#!/bin/bash
# COP tool stub — actual state update happens in auspex's event interceptor.
# This script exists so omegon's armory runner has something to execute.
# The tool args are passed as the first argument (JSON string).
echo "ok"
